/**
 *
 */
/*
package com.spar.hcl.facades.populators;

import de.hybris.platform.converters.Populator;
import de.hybris.platform.servicelayer.dto.converter.ConversionException;

import com.spar.hcl.core.model.service.delivery.SparServiceAreaModel;
import com.spar.hcl.facades.service.delivery.data.SparServiceAreaData;


 *//**
 * @author tanveers
 *
 */
/*
 * public class ServiceAreaPopulator implements Populator<SparServiceAreaModel, SparServiceAreaData> {
 * 
 * @Override public void populate(final SparServiceAreaModel source, final SparServiceAreaData target) throws
 * ConversionException { if (null != source) { target.setAreaId(source.getAreaId());
 * target.setDisplayName(source.getDisplayName()); target.setArea(source.getArea());
 * target.setPincode(source.getPincode()); target.setDefaultStore(source.getDefaultStore());
 * target.setDefaultCncCenter(source.getDefaultCncCenter()); target.setCity(source.getCity());
 * target.setActive(source.getActive()); }
 * 
 * } }
 */
